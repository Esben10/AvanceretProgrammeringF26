## Forberedelsesspørgsmål til uge 3 - Concurrency

**Besvar følgende spørgsmål ved at researche online.**  
Du kan bruge Java-dokumentationen, tutorials, artikler eller videoer.  
Formålet er, at du forstår begreberne og kan forklare dem med dine egne ord.

---

### 1. Hvad er forskellen på *concurrency* og *parallelisme* i Java?
- Hvordan adskiller de sig i forhold til CPU-udnyttelse og opgavehåndtering?

### 2. Hvad er forskellen på at implementere `Runnable` og at udvide `Thread` i Java?
- Hvornår bør man vælge den ene frem for den anden?

### 3. Hvad er en *race condition*?
- Hvordan opstår den, og hvilke problemer kan den medføre?

### 4. Hvad er en *deadlock*?
- Hvordan kan den opstå mellem tråde, og hvordan kan man undgå den?

### 5. Hvad er en *thread pool* i Java, og hvordan hænger det sammen med `ExecutorService`?
- Hvad er ideen med at genbruge tråde fremfor at oprette nye?
- Hvilke klasser eller interfaces i Java bruges typisk til at arbejde med thread pools?
- Hvordan skaber man en thread pool med `ExecutorService`?
- *(Bonus: Find et simpelt eksempel på brug af `ExecutorService` og `submit()` eller `execute()`.)*

---

> Du behøver ikke kunne alt i detaljer – det vigtigste er, at du har læst og tænkt over begreberne.
